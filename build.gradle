apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'

repositories {
    flatDir{
        dirs 'lib'
    }
    mavenCentral()
}

dependencies {
    classpath 'ojd'
    testCompile group: 'junit', name: 'junit', version: '4.11'
}